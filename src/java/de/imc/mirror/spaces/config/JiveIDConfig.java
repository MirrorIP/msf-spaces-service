package de.imc.mirror.spaces.config;

/**
 * Definition of JiveID type identifier used in the annotations of the model classes.
 * The selection is random, but may not interfere with other JiveIDs already used by
 * openfire.
 * @author simon.schwantzer(at)im-c.de
 *
 */
public interface JiveIDConfig {
	public int TEAM_SPACE = 2201;
	public int ORGA_SPACE = 2202;
}
